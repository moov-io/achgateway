# generated-from:61c921098916a5e12d9002cea7316299dc45fe9e9440c7986d52d9040ab4dff7 DO NOT REMOVE, DO UPDATE

name: CodeQL Analysis

on:
  push:
  pull_request:
  schedule:
    - cron: '0 0 * * 0'

jobs:
  CodeQL-Build:
    strategy:
      fail-fast: false
    runs-on: ubuntu-latest
    steps:
      - name: Set up Go 1.x
        uses: actions/setup-go@v2
        with:
          go-version: ^1.16
        id: go

      - name: Checkout repository
        uses: actions/checkout@v2
        with:
          fetch-depth: 0

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v1
        with:
          languages: go

      - name: Install
        run: make install

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v1
